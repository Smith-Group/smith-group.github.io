# Close a PDB trajectory file

Close a PDB trajectory file

## Usage

``` r
pdb_traj_close(traj_data)
```

## Arguments

- traj_data:

  list structure with data necessary for reads

## Value

No return value, called for side effects.
