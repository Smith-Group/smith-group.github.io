# Convert a list of subset groups into a matrix with group indices

Convert a list of subset groups into a matrix with group indices

## Usage

``` r
subset_idx_to_grouping_mat(subset_idx)
```

## Arguments

- subset_idx:

  list of lists of integers giving members of each group

## Value

a matrix with row for each outer list and group assignment for each
ensemble member
