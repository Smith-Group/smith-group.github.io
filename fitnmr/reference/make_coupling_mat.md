# Make a multiplet matrix with weights and scalar coupling coefficients

Make a multiplet matrix with weights and scalar coupling coefficients

## Usage

``` r
make_coupling_mat(sc_names)
```

## Arguments

- sc_names:

  character vector with names of scalar couplings
