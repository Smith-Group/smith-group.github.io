# Collapse first two dimensions of an array

Collapse first two dimensions of an array

## Usage

``` r
collapse_array(arr, idx_list, func = `+`)
```

## Arguments

- arr:

  two or three dimensional array with the first two dimensions having
  the same length

- idx_list:

  list of character or integer indices to accumulate using func

- func:

  function to apply
