# Determine a matrix of fractional peak overlap

Determine a matrix of fractional peak overlap

## Usage

``` r
spec_overlap_mat(peak_int_list)
```

## Arguments

- peak_int_list:

  list of peak intensity arrays
