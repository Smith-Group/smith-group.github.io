# Collapse strings of repeated NAs in a vector with numeric names

Collapse strings of repeated NAs in a vector with numeric names

## Usage

``` r
collapse_na(x)
```

## Arguments

- x:

  1D array with dimnames having numeric positional values
