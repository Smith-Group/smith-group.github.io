# Convert between proton atom/residue name pairs

Convert between proton atom/residue name pairs

## Usage

``` r
amber_to_pdb

pdb_to_amber

charmm_to_pdb

pdb_to_charmm
```

## Format

An object of class `character` of length 161.

An object of class `character` of length 161.

An object of class `character` of length 161.

An object of class `character` of length 161.
