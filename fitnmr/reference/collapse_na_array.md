# Collapse blocks of NAs in an array with numeric dimension names

Collapse blocks of NAs in an array with numeric dimension names

## Usage

``` r
collapse_na_array(x)
```

## Arguments

- x:

  array with names having numeric positional values
