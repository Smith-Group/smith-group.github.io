# Calculate matrix of a values from a matrix of g values

Calculate matrix of a values from a matrix of g values

## Usage

``` r
g_matrix_to_a_matrix(g_matrix, a_coef)
```

## Arguments

- g_matrix:

  matrix of g values with columns associated with different groupings

- a_coef:

  coefficients used for calculating a values
