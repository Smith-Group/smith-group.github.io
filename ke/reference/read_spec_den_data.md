# Read data for calculating spectral density functions

Read data for calculating spectral density functions

## Usage

``` r
read_spec_den_data(prefix_path)
```

## Arguments

- prefix_path:

  to prefix of four CSV files

## Value

a list with elements: `atom_pairs`, `groupings`, `a_coef`, and
`lambda_coef`
