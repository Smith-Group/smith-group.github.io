# Split string of scalar coupling names

The only currently implemented way of splitting string is using
whitespace

## Usage

``` r
split_coupling_names(name_char)
```

## Arguments

- name_char:

  single string (character vector of length 1) with scalar coupling
  names
