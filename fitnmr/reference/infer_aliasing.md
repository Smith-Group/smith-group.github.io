# Infer which dimension was directly acquired

Infer which dimension was directly acquired

## Usage

``` r
infer_aliasing(fheader, phase_tolerance = 10)
```

## Arguments

- fheader:

  matrix of generalized ND parameters

- phase_tolerance:

  tolerance in degrees for detecting half-dwell delay
